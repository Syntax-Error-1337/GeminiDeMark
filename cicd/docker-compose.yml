version: '3.8'

services:
  frontend:
    build:
      context: ../frontend-nextjs
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=/api/auth
    image: ghcr.io/syntax-error-1337/geminidemark-frontend:main
    restart: always
    environment:
      - NODE_ENV=production
    networks:
      - app-network

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    image: ghcr.io/syntax-error-1337/geminidemark-backend:main
    restart: always
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_HOST=db
    depends_on:
      - db
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app-network

  nginx:
    image: owasp/modsecurity-crs:nginx
    restart: always
    ports:
      - "${HTTP_PORT:-1004}:80"
      - "${HTTPS_PORT:-1005}:443"
    environment:
      - MODSEC_RULE_ENGINE=DetectionOnly
      - DOMAIN_NAME=${DOMAIN_NAME:-localhost}
    volumes:
      - ./nginx/default.conf.template:/etc/nginx/templates/conf.d/default.conf.template
      - ./nginx/certs:/etc/nginx/certs
    depends_on:
      - frontend
      - backend
    networks:
      - app-network

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: watermark_db
      MYSQL_USER: hermes
      MYSQL_PASSWORD: testing123
      MYSQL_ROOT_PASSWORD: rootpassword
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  mysql_data:
